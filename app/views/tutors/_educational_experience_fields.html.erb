<fieldset>
	<%= f.text_field :university, placeholder: "#{t('tutors.show.university')}" %>
	<%= f.text_field :major, placeholder: "#{t('tutors.show.major')}" %>
	<%= f.text_field :minor, placeholder: "#{t('tutors.show.minor')}" %>
	<%= f.hidden_field :_destroy %>
	<%= link_to "[remove]", "#", class: "remove_fields" %>
</fieldset>
